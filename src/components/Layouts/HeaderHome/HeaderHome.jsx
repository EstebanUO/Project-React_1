import React from "react";
import { Ancla } from "../UI/Logo/Logo";
import { Logo } from "../UI/Logo/Logo";

export const HeaderHome = () => {
  return (
    <header className="headerHome">
      <Logo />
      <Ancla />
    </header>
  );
};
